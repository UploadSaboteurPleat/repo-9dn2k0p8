# TODO — дальнейшие задачи

Список сгруппирован по приоритету. На момент передачи обязательных (блокирующих)
задач нет — сайт готов и развёрнут. Ниже — улучшения.

## Критично (сделать в первую очередь при продолжении)

- [ ] Свести/смержить ветки в `main`: открытые PR `#9` (слайдеры), `#10` (фото),
      `#11` (логотип) дублируются веткой `devin/handoff-project-state`, которая уже
      содержит всё вместе. Решить: слить handoff-PR в `main` и закрыть `#9/#10/#11`,
      либо мержить их по отдельности. Сейчас в `main` только базовая версия (PR #7).
- [ ] Зафиксировать продакшен-домен и настроить деплой на хостинг клиента
      (сейчас демо живёт на превью-домене Devin).

## Желательно

- [ ] Оптимизировать изображения (сжать JPEG, добавить `loading="lazy"`,
      рассмотреть WebP) — некоторые фото 250–630 КБ.
- [ ] Заменить favicon (`images/logo-samrau.svg`) на брендовый вордмарк/иконку.
- [ ] Добавить SEO-метатеги Open Graph / Twitter Card и `favicon` разных размеров.
- [ ] Проверить доступность (контраст, фокус-стили, alt-тексты) и прогнать Lighthouse.
- [ ] Добавить мета-тег `og:image` со скриншотом/логотипом для красивых ссылок.

## Можно позже

- [ ] Вынести CSS и JS из `index.html` в отдельные файлы (`styles.css`, `app.js`),
      если сайт будет расти.
- [ ] Локально вшить шрифты (Inter, Manrope) для офлайн-работы и независимости от
      Google Fonts.
- [ ] Добавить форму обратной связи / заявки (потребует бэкенда или внешнего сервиса).
- [ ] Подключить аналитику (Яндекс.Метрика / GA), если потребуется.
- [ ] Мультиязычность (если нужна англоязычная версия для B2B-партнёров).

## Где начинать смотреть код

1. `index.html` — единственный файл сайта.
2. Секция проектов «до/после»: разметка ~строка 1650 (`id="proekty"`),
   логика слайдера ~строка 1940 (`querySelectorAll('.ba-compare[data-ba]')`),
   стили слайдера ~строка 692 (`.ba-compare`).
3. `PROJECT_STATUS.md` — общая карта проекта и решений.
